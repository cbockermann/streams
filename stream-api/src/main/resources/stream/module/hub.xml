<module>
    
    <queue id="output.module${queuecopy.id}" capacity="${hub.queuecapacity${include.id}}" queuecopies="[${hub.input.copies${include.id}}]:[${include.copies}]"/>
    
	<process input="output.module${copy.id}" copies="[${hub.input.copies${include.id}}]:[${include.copies}]">
	    <stream.flow.Emitter sinks="input.module${queuecopy.id}"  queuecopies="[${hub.output.copies${include.id}}]:[${include.copies}]" />
	</process>
	
	<queue id="input.module${queuecopy.id}" capacity="${hub.queuecapacity${include.id}}" queuecopies="[${hub.output.copies${include.id}}]:[${include.copies}]"/>
    
</module>