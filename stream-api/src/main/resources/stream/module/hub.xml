<module>
    
    <queue id="${copy.id}" capacity="${hub.queuecapacity${include.id}}" copies="[${hub.input.copies${include.id}}]:[${include.copies}]"/>
    
	<process input="${copy.id}" copies="[${hub.input.copies${include.id}}]:[${include.copies}]">
	    <stream.flow.Emitter sinks="[${hub.output.copies${include.id}}]:[${include.copies}]" />
	</process>
	
	<queue id="${copy.id}" capacity="${hub.queuecapacity${include.id}}" copies="[${hub.output.copies${include.id}}]:[${include.copies}]"/>
    
</module>